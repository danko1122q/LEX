@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Tambahkan dependency di sini jika proyek Anda membutuhkannya, contoh:
# find_dependency(SomeOtherLib REQUIRED)

# Sertakan file target yang dihasilkan oleh perintah install(EXPORT ...)
include("${CMAKE_CURRENT_LIST_DIR}/lexTargets.cmake")

# Periksa apakah semua komponen ditemukan
check_required_components(lex)