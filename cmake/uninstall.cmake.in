# uninstall.cmake.in - Template untuk skrip uninstalasi CMake
#
# File ini akan dikonfigurasi oleh CMake untuk menghasilkan 'uninstall.cmake'
# di direktori build. Skrip ini membaca 'install_manifest.txt' yang berisi
# daftar semua file yang telah diinstal oleh 'make install'.

# Periksa apakah file manifest ada. Jika tidak, hentikan dengan pesan kesalahan.
if(NOT EXISTS "@CMAKE_INSTALL_MANIFEST_FILE@")
  message(FATAL_ERROR "Cannot find install manifest: \"@CMAKE_INSTALL_MANIFEST_FILE@\"")
endif()

# Baca semua baris (nama file) dari file manifest ke dalam variabel 'files'.
file(STRINGS "@CMAKE_INSTALL_MANIFEST_FILE@" files)

# Loop melalui setiap file dan hapus.
foreach(file ${files})
  message(STATUS "Removing: ${file}")
  # Hanya coba menghapus jika file atau symlink benar-benar ada.
  if(IS_SYMLINK "${file}" OR EXISTS "${file}")
    file(REMOVE ${file})
  endif()
endforeach()